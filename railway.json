{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile && pnpm db:generate && pnpm --filter @sentinel/shared build && pnpm --filter @sentinel/api build"
  },
  "deploy": {
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyMaxRetries": 10,
    "restartPolicyType": "ON_FAILURE",
    "startCommand": "if [ -n \"$DATABASE_URL\" ]; then pnpm db:migrate:deploy; fi && pnpm --filter @sentinel/api start"
  }
}
